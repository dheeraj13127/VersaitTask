import React, { ReactElement, FC } from "react";

const Temp: FC<any> = (): ReactElement => {
    return (
        <div className="dashboardContainer">
            
        </div>
    );
};

export default Temp;